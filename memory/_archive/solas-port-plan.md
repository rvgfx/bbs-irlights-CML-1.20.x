---
name: solas-port-plan
description: "IRLite -> Solas port roadmap (TAKE 2 — a previous attempt failed leaving no traces) — phases 0-5 (recon done; SSBO+diffuse/specular, shadows, reduced-res VL, settings UI, optional outline, diff+patcher), each a SEPARATE session; per-phase ops/test/risks and current status"
metadata: 
  node_type: memory
  mod_scope: shader-inject
  ported_from: IRLite
  consolidated: 2026-06-18
  type: project
  originSessionId: 8b9db109-b8c5-4c3b-87b4-9459a18f4762
---

Roadmap for porting the IRLite inject to Solas (by Septonious), fifth target after IterationRP, Photon, ComplementaryReimagined, BSL. Parent: [[MEMORY]]. WHERE/HOW to hook = [[solas-pipeline]] (read it first every phase). Port SOURCE = the BSL Modification inject (Shadres/Modification/BSL/shaders/lib/irlite/irlite_lights.glsl, 1041 lines — most recent lineage: raw-linear colour in all three halves, self-contained world-space spec, self-made discriminators, VL reduced-res rework, outline, all bugfixes) + its programs/deferred2.glsl + wrappers. Folder/test loop = [[sync-workflow]]. Final output = patches/solas.irlpatch ([[patcher]]).

TAKE-2 CONTEXT (user statement 2026-06-12): a previous Solas port attempt FAILED; any memory about it would be unreliable — but NO records of it exist anywhere (no memory files, no commits, no patch, no Modification folder, Original greps clean for irlite/irl_ markers). This roadmap starts from zero with the IterationRP take-3 discipline: fresh Modification copy, ONE phase per session, an IN-GAME GATE after every phase (user verifies before the next phase starts), commit per phase on user confirmation ([[commit-checkpoints]]).

GROUND RULES (user-set, same as all ports): work entirely in Shadres/Modification/Solas (clean copy of Shadres/Original/Solas made + count-verified 384=384 on 2026-06-12); ALL phases hand-edit Modification; diff + build the .irlpatch ONLY at the very end (Phase 5). The Java addon and the patcher applier are pack-agnostic and DO NOT change. GIT: `shaders/` is globally gitignored (line 123) — use `git add -f` for pack folders.

INJECT FILE: shaders/lib/irlite/irlite_lights.glsl. Self-contained IRLITE_* defines at top; in-file #extension lines (SSBO + cube_map_array + texture_gather); raw-linear colour (Solas hook sits inside gbuffersLighting's internal linear window — zero colour adaptation vs BSL source). Discriminators: surface half `#ifdef IRLITE_SURFACE_PASS` (defined by the gbuffersLighting include op) + skip gate `!DH_TERRAIN && !DH_WATER && !VOXY_OPAQUE && !VOXY_TRANSLUCENT` (pack programs self-define these — no flag ops); nonTerrain = `GBUFFERS_ENTITIES || GBUFFERS_BLOCK || GBUFFERS_HAND` compile-time; VL march half `#ifdef IRLITE_VL_PASS` (defined by our deferred2); composite half (upsample + outline) `#ifdef IRLITE_COMPOSITE_PASS` (defined by the composite1 include op).

=== PHASES ===

PHASE 0 — Recon + scaffold + plan.  STATUS: DONE 2026-06-12 (this session).
- Read memory; verified the failed attempt left no traces (Original marker-clean; 27 late-dated files = pack's own Mar/Apr update churn); full pack recon (agent sweep + own verification of every load-bearing anchor); copied Solas -> Modification (384 files verified); wrote [[solas-pipeline]] + this plan. No GLSL written yet. Commit proposed to user.

PHASE 1 — Core SSBO library + DIFFUSE + SPECULAR (shadow stubs, no VL).  STATUS: VERIFIED in-game 2026-06-12 (user "всё работает" after the two load-failure bugfixes below) — COMMITTED 9da3455 (lib + gbuffersLighting hooks + properties + tools/irislex).
- UNIFORM AUDIT RESULT: all 7 active includers (terrain/entities/hand/water/block/basic/textured) declare cameraPosition + gbufferModelViewInverse + even gbufferModelView in their FSH halves — NO per-program uniform ops needed (dh_*/voxy* includers are gated out by IRLITE_ACTIVE and need nothing).
- LANDED (Shadres/Modification/Solas): created lib/irlite/irlite_lights.glsl (1201 lines — the FULL BSL lib adapted in one go, not grown-per-phase; shadow/VL/outline halves carried but compiled out by their defines): IRLITE_SHADOWS + IRLITE_VOLUMETRIC + IRLITE_VL_SHADOWS commented OFF (Phases 2/3 flip them); IRLITE_VL_RESOLUTION changed from BSL's `50 // [100 50 25]` to float `0.5 // [1.0 0.5 0.25]` (Complementary direct-ref mechanism); IRLITE_ACTIVE gate -> `!DH_TERRAIN && !DH_WATER && !VOXY_OPAQUE && !VOXY_TRANSLUCENT` (pack self-defines); outline entity test pre-adapted to the Solas colortex3-alpha hypothesis (`.a > 0.975`; banner lists the Phase-4.5 verification homework incl. composite1's own colortex3 ~L49 + uniform availability); VL banner notes underwater parity moves MARCH-SIDE; everything else VERBATIM from BSL (shadow block, spec GGX, merged loop, VL march, 5x baselines).
- Hooked lib/lighting/gbuffersLighting.glsl ×2: `#define IRLITE_SURFACE_PASS` + include inserted before the `void gbuffersLighting(` signature line; after the `vec3 finalColor = ...` line the gated block — compile-time irliteNonTerrain via `GBUFFERS_ENTITIES||GBUFFERS_BLOCK||GBUFFERS_HAND`, one irlite_lightSurface(worldPos, newNormal, viewPos, ...) call, `finalColor += albedo.rgb * (1.0 - metalness) * (IRLITE_INTENSITY * irliteDiffuse)` under bare `#ifdef IRLITE_DIFFUSE` + `finalColor += (IRLITE_INTENSITY * IRLITE_SPECULAR_INTENSITY) * irliteSpec` under bare `#ifdef IRLITE_SPECULAR` (BSL call-site parity; albedo.rgb is linear-unlit at that point, (1-metalness) = the pack's own diffuse damping). Preprocessor balance verified: lib 30/30, gbuffersLighting 32/32.
- shaders.properties ×1 (after the load-failure bugfix, see below): `iris.features.optional=CUSTOM_IMAGES SSBO` (replace only).
- BUGFIX (2026-06-12, same session — user report "не компилируется, ошибок в игре нет"): first load threw `IllegalStateException: Custom images are being used, but the feature flag for custom images hasn't been set` (latest.log, ShaderPack.java:190). ROOT CAUSE proven via Iris 1.7.2 source + an OFFLINE JCPP REPLICA of PropertiesPreprocessor.process (tools: %TEMP%\irisprop\PropTest.java + jcpp-1.4.14 from the gradle cache): Iris parses iris.features/sliders/profiles/screens from the RAW un-preprocessed properties (original.forEach) where Java-Properties duplicate-key = LAST-IN-FILE WINS — my BSL-style `#ifndef VX_SUPPORT` twin (`iris.features.optional=SSBO`) sat lower in the file and OVERRODE the real `CUSTOM_IMAGES SSBO` line, while image.* (preprocessed-parsed, VX_SUPPORT default ON) stayed active -> flag/image mismatch. FIX: removed the whole #ifndef block — the single replaced line suffices in every branch state (raw parse ignores conditionals). Verified offline: raw file has exactly one flags line; preprocessed output consistent with VX_SUPPORT both ON and OFF. The same raw-parse fact recorded in [[solas-pipeline]] hook 3 (matters for Phase 4 sliders/screens too). CROSS-PORT: BSL carries the same latent bug (its #ifndef MULTICOLORED_BLOCKLIGHT twin overrides flags to SSBO-only; crashes the moment a user enables MCBL whose custom images then activate) — warning logged in [[bsl-pipeline]], fix on next BSL touch.
- BUGFIX 2 (2026-06-12, same session — second load failure after bugfix 1): `RuntimeException: GLSL source pre-processing failed / InternalException: Bad token [Unknown8212@1787,47]` from JcppProcessor at program read. ROOT CAUSE: the lib's /* */ banner contained the text `dh_*/voxy*` — the `*/` inside it TERMINATES the block comment early, the rest of the banner lexes as GLSL tokens and the first em dash (codepoint 8212) is an unknown token for JCPP. JCPP LEXES THE WHOLE FILE INCLUDING compiled-out #ifdef regions — broken comments/odd tokens crash the pack even in dead code (this is the general mechanism behind "breaks by mere presence"; the Complementary port already learned it as "header comment escapes */" for deferred2.glsl). Em dashes INSIDE intact comments are safe (the BSL lib ships 63 non-ASCII comment lines). FIX: `dh_*/voxy*` -> `dh_* and voxy*`. RULE: never write `*/` inside a block comment (glob patterns like `x_*/y_*` are the trap); // line comments are immune.
- VALIDATION TOOLING (persisted to tools/irislex/, reusable for ALL ports as a pre-sync gate): GlslTest.java = replica of Iris 1.7.2 JcppProcessor.glslPreprocessSource (marker-replace + KEEPCOMMENTS + token loop); PropTest.java = replica of PropertiesPreprocessor.process for shaders.properties; lextest.ps1 = include-resolving driver that concatenates every root program (like ProgramSet) and lexes it (-ShadersRoot param). Classpath = jcpp-1.4.14 + slf4j-api from the gradle cache (paths hardcoded in lextest.ps1 — refresh if the cache moves). Proven: fixed pack 57/57 clean; negative control (broken line restored) reproduces the EXACT in-game signature (Unknown8212, column 47) on all 11 lib-including programs.
- TEST (in-game gate, user): pack compiles at load (SSBO at 130, no in-pack SSBO precedent — front-loaded risk, but BSL@120/Compl@130 both proved the Iris-transpile path); point/spot falloff+cone correct; terrain/entities/hand/water/particles lit; entities-only lightMask gates correctly (BBS replay model = entities, model-block = block, held = hand); brightness/hue sanity vs BSL_IRLite side-by-side (same linear convention — expect defaults to carry); pack's own floodfill/COLORED_LIGHTING + profiles unaffected.
- RISKS: SSBO first-compile; floodfill interplay (ours adds AFTER pack light mixing — visual double-light near emitters is expected/fine); Iris option-scan now sees all IRLITE_* defines incl. the carried outline half (broke ONLY IterationRP historically; Solas is BSL/Compl-family where it shipped fine — if Solas misbehaves pack-globally, strip the outline half first, the IterationRP bisect lesson).

PHASE 2 — SHADOWS.  STATUS: VERIFIED in-game 2026-06-12 (user "всё норм") — COMMITTED 4a81bdb.
- LANDED: `//#define IRLITE_SHADOWS` -> `#define IRLITE_SHADOWS` in lib/irlite/irlite_lights.glsl (one line). The block it activates was ported verbatim from BSL in Phase 1: gather-PCF spot atlas + dominant-axis point cube-array, texel floors, normal-offset, PCSS tiers, vogel; in-file extensions cube_map_array + texture_gather; IRLITE_COMPILE_SHADOWS escape-hatch gate as-is. The Phase-1 `#ifdef IRLITE_SHADOWS` attenuation call in irlite_lightSurface now activates.
- TEST: compiles (samplerCubeArray + textureGather at #version 130 via in-file extensions — Compl-proven at 130, low risk; the settings toggle strips the construct as the escape hatch); no 6-point star on point lights (dominant-axis depth); spot atlas tile maps correctly; terrain + BBS replay model receive shadows; acne check (normal-offset + texel floors ported).

PHASE 3 — VOLUMETRIC (reduced-res from day one — the BSL/Compl perf lesson is pre-applied).  STATUS: LANDED 2026-06-12 (same session), lex-gate 59/59 clean + properties-preprocess check OK, synced to run/shaderpacks (392 files) — AWAITING in-game verification.
- LANDED (11 files):
  (1) +programs/deferred2.glsl — adapted from the BSL source (Shadres/Modification/BSL/shaders/program/deferred2.glsl): line 1 `#define IRLITE_VL_PASS`; includes /lib/common.glsl (Solas program idiom — needed for the TAA define; common.glsl is settings+helpers, declares no uniforms) + the irlite lib INSIDE the FSH half; own uniforms (frameCounter, isEyeInWater, cameraPosition, gbufferProjectionInverse, gbufferModelViewInverse, depthtex0, noisetex); INLINE position reconstruction with the bobbing-correct `mat3(MVI) * viewPos + MVI[3].xyz`; texture2D NOT texelFetch (reduced viewport vs full-res depthtex0); dither = noisetex .b + `fract(d + 1.61803398875 * mod(float(frameCounter), 3600.0))` under `#ifdef TAA` (composite1 idiom); UNDERWATER PARITY MARCH-SIDE (`*= pow(vec3(0.80,0.87,0.97)*0.55, vec3(2.0))` at isEyeInWater==1, =0 at ==2); body gated `#if defined IRLITE_ACTIVE && defined IRLITE_VOLUMETRIC` with zero-write fallback; `/* RENDERTARGETS: 10 */`; FSH/VSH combined (`in/out vec2 texCoord`, VSH = gl_TextureMatrix[0]*gl_MultiTexCoord0 + ftransform — composite1 parity). Banner comments are ASCII-dash and contain NO `*/` globs (the bugfix-2 rule).
  (2) +6 wrappers shaders/deferred2.{fsh,vsh} (OVERWORLD — root IS overworld) + world1/ (END) + world-1/ (NETHER): `#version 130` + FSH|VSH + dim define + include, NO trailing newline (pack wrapper parity, verified on deferred.fsh: 84 bytes no-NL).
  (3) programs/composite1.glsl ×2: include op after the unique unconditional `uniform sampler2D colortex0;` line (FSH half, before the pack's gated uniform block) — body `#define IRLITE_COMPOSITE_PASS` + include; upsample op BEFORE the tab-indented `	/* DRAWBUFFERS:0 */` line — gated `color += texture2D(colortex10, texCoord).rgb;` (bilinear by default; uniform-free — colortex10 decl comes from the lib's composite-gated block).
  (4) programs/final.glsl: `const int colortex10Format = RGB16F;` INSIDE the /* */ block after the colortex7Format line.
  (5) shaders.properties: 3 toggle lines `program.world{0,-1,1}/deferred2.enabled=IRLITE_VOLUMETRIC` after the unique `program.world1/shadowcomp.enabled=VX_SUPPORT` + comment + `size.buffer.colortex10=IRLITE_VL_RESOLUTION IRLITE_VL_RESOLUTION` (verified via PropTest: expands to `0.5 0.5`; toggle lines expand to `enabled=1` when the bool option is ON and stay `=IRLITE_VOLUMETRIC` when OFF — both forms parse correctly, the pack's own `=SSAO` toggles ride the same mechanism).
  (6) lib: IRLITE_VOLUMETRIC + IRLITE_VL_SHADOWS flipped ON. Zero-beam skip + VL_STEPS 10 + VL_SHADOW_STRIDE 2 shipped with the lib in Phase 1.
- TEST: compiles in all 3 dims (wrappers!); beams/glow visible, reduced-res upsample clean (Half default); FPS sane inside light radius; VL shadows occlude beams; underwater dim / lava zero; behind-camera bake-cull shafts known issue ([[shader-volumetric]]) — unchanged here.
- RISKS: size.buffer has no pack precedent (Iris-standard, Photon/BSL/Compl-proven — low); colortex10 sampler filtering assumed LINEAR-default (ports precedent).

PHASE 4 — SETTINGS UI.  STATUS: LANDED 2026-06-12 (after Phase 4.5 — order swapped on user request), PropTest + lex-gate 59/59 clean, synced (4 files) — AWAITING in-game verification. (Phase 4.5 verified + committed 8f6c900 before this.)
- LANDED (4 files):
  (1) lib: IRLITE_OUTLINE default flipped back to OFF (port-consistent; verified working in 4.5, now UI-toggled — flip the default on request).
  (2) shaders.properties ×2 edits: screen.LIGHTING line (the unique triple-tail `VANILLA_AO SSAO AO_STRENGTH`) appended ` [IRLITE_SETTINGS]` + the 7-line screens block inserted right after it (IRLITE_SETTINGS top: DIFFUSE + INTENSITY + 5 sub-screens SPECULAR/SHADOW/VL/TOON/OUTLINE, pack `<empty>` spacer idiom); `sliders=` tail (`WAVING_AMPLITUDE FIREFLIES_BRIGHTNESS`, unique) appended 20 numeric IRLITE_* (VL_RESOLUTION + OUTLINE_TARGET stay value-cycle buttons).
  (3+4) lang/en_US.lang + lang/ru_RU.lang (FIRST pack with a Russian block — pack ships ru_RU, ends `option.FIREFLIES_BRIGHTNESS=Яркость Светлячков` L746): full IRLite blocks appended after the true last line — 6 screens, 28 options + comments, value labels (SHADOW_QUALITY 0-4, VL_RESOLUTION .1.0/.0.5/.0.25 Full/Half/Quarter, OUTLINE_TARGET 0/1). §-groups: §6 master / §b spec / §3 shadows / §d vl / §a toon / §e outline. ASCII hyphens in comments (not em dashes — paranoia after bugfix 2, though lang is not lexed).
- RAW-PARSE note (bugfix-1 fact applied): screens/sliders are parsed from the RAW properties — tail-replace edits modify the existing lines, never add duplicates; macro expansion of option names in the PREPROCESSED copy is irrelevant to them (the pack's own screen lines ride the same behavior).
- TEST: settings tree navigable (Lighting -> IRLite Lights -> 5 sub-screens) in BOTH languages; toggles recompile correctly; IRLITE_SHADOWS OFF still compiles (strips cube-array); IRLITE_VOLUMETRIC OFF disables the deferred2 pass (program toggle); IRLITE_OUTLINE ON via UI re-verifies 4.5; pack profiles unaffected (no IRLITE_* in profile.* lines).

PHASE 4.5 — OUTLINE (pulled BEFORE Phase 4 on user request "некст экстра фаза это аутлайны").  STATUS: LANDED 2026-06-12 (same session), lex-gate 59/59, lib balance 31/31, synced — AWAITING in-game verification. IRLITE_OUTLINE define currently ON for the gate test (decide shipped default in Phase 4; other ports ship OFF).
- MASK FACTS (hypothesis CONFIRMED + refined against pristine): colortex3 alpha — terrain ≤0.95; gbuffers_entities vec4(0,0,0,1) (BBS replay models); gbuffers_block (encodeNormal, 0.0, 1.0) — BBS MODEL-BLOCKS + chests/banners IN THE MASK (bonus vs BSL/Compl where blocks were excluded); gbuffers_water writes a==1.0 TOO but with b = water*0.4+0.4 >= 0.4 (glass incl.) => test = `a > 0.975 && b < 0.1` (irlite_outlineEntity, excludes water/glass); hand writes NO colortex3 + skipped by z0>0.56 (constant confirmed in composite3); particles go to colortex1 not 3; deferred passes write 0/4/5 only — mask survives to composite1.
- LANDED (2 files):
  (1) lib: IRLITE_OUTLINE flipped ON; entity test reworked to the a/b form; the outline half's `uniform sampler2D colortex3;` replaced by a MIRROR-GATED dependency block `#if !defined VL && !defined LPV_FOG && !defined NETHER_SMOKE` declaring far/near, viewWidth/viewHeight, colortex3, depthtex0, gbufferProjectionInverse, gbufferModelViewInverse — composite1 declares ALL of those only inside its own `#if defined VL || defined LPV_FOG || defined NETHER_SMOKE` block (L14-77), the mirror is exact (flat gate, no deeper nesting).
  (2) composite1.glsl: include RELOCATED from after-colortex0 to BEFORE the unique `// Global Variables //` line — i.e. BELOW the pack's conditional uniform block: with the pack gate ACTIVE the lib's functions would otherwise reference near/far/depthtex0 BEFORE their declaration (GLSL has no hoisting — caught pre-game by review, would have been a compile error with pack-VL on). Ink op inserted BEFORE the Phase-3 VL upsample block (additive => order cosmetic; BSL parity): inline ndc->view->player reconstruction (MVI[3].xyz bobbing-correct), derivatives OUTSIDE the z-gate branch (undefined in non-uniform control flow — BSL comment carried), camera-facing flip + entity test + `color += IRLITE_INTENSITY * irlite_outlineInk(...)` inside `irliteZ0 < 1.0 && irliteZ0 > 0.56`.
- PHASE-5 IMPACT: composite1 include anchor = before-op on `// Global Variables //` (NOT after-colortex0); composite1 now carries THREE ops (include, ink+VL combined body or two ops before `	/* DRAWBUFFERS:0 */`).
- TEST: compiles with pack VL ON and OFF (the mirror-gate both ways!) and with IRLITE_OUTLINE toggled OFF; backlit BBS model shows contour + feet-border; model-blocks/chests ink too; WATER does NOT ink; frontal light draws no line (WRAP); terrain clean at TARGET=1, lines at TARGET=0; no NaN speckles.
- IterationRP's outline-breaks-pack history does NOT generalize (BSL/Compl shipped fine; Solas lexes clean with the block present since Phase 1).

PHASE 5 — DIFF + PATCHER.  STATUS: DONE 2026-06-12 — patch generated + VALIDATED with the real applier (19/19 anchors matched exactly once; diff vs Modification EMPTY over 392 files), COMMITTED 3966a9c. (Phase 4 committed 668d24a — landed + offline-validated; the user moved straight to Phase 5, in-game settings check still pending — regen via the script if Phase-4 fixes emerge.)
- DELIVERABLE: patches/solas.irlpatch (1686 lines, 19 ops, @name "Solas lights" / @target Solas / @marker IRLITE) + tools/gen-solas-patch.ps1 (body-splice from Modification with boundary asserts, EmitFile trailing-newline fix carried from the Complementary gen). FIRST PATCH WITH A RUSSIAN LANG OP.
- OP INVENTORY (19): +file ×8 — lib/irlite/irlite_lights.glsl, programs/deferred2.glsl, 6 wrappers (root+world1+world-1, no trailing newline). @file gbuffersLighting.glsl ×2: before `void gbuffersLighting(` -> define+include+blank; after the finalColor line -> the compute+add block (leading-blank body). @file programs/composite1.glsl ×2: before `// Global Variables //` -> the comment+define+include block (trailing-blank body); before `\t/* DRAWBUFFERS:0 */` (tab as \t escape) -> ink block + blank + VL upsample block (trailing-blank body). @file programs/final.glsl ×1: after the colortex7Format line -> colortex10Format RGB16F. @file shaders.properties ×4: replace `iris.features.optional=CUSTOM_IMAGES` -> +` SSBO` (ONE op — raw-parse fact); after `program.world1/shadowcomp.enabled=VX_SUPPORT` (pristine's last line) -> 3 toggles + blank + comment + size.buffer; replace `VANILLA_AO SSAO AO_STRENGTH` -> tail+[IRLITE_SETTINGS] + the 6 screen lines; replace `WAVING_AMPLITUDE FIREFLIES_BRIGHTNESS` -> +20 sliders. @file lang/en_US.lang ×1 + lang/ru_RU.lang ×1: after the true-last-line anchors -> the full L10n blocks (the ru anchor is built FROM the Modification file inside the generator — no cyrillic literals in the .ps1, dodges PS 5.1 encoding).
- Validation: javac PatchHarness + the 4 pure patcher classes -> apply to Shadres/Original/Solas -> `git -c core.autocrlf=false diff --no-index --ignore-cr-at-eol` vs Modification = EMPTY, 392=392 files.
- PORT COMPLETE (phases 0-5 + 4.5; in-game verified: 1,2,3,4.5; Phase 4 offline-validated, in-game pending).

CROSS-PORT NOTES: Photon still owes the zero-beam skip ([[photon-bugfix]]); nothing from Solas blocks it. After Phase 5, MEMORY.md shader-doc disclaimers don't change (Solas docs are new-gen from day one).

---

## Связь с IRL-redactor (объединение памяти 2026-06-18)

- **Применимость:** shader-inject — Применимо к shader-inject (содержимое .irlights / авторинг GLSL-инжекта в шейдерпак): IRLite-side источник правды, синкается одно-направленно в redactor через copy-patches.ps1; Java-аддон и patcher-applier пак-агностичны и не затрагиваются. Среди перечисленных redactor-канон-памятей прямого аналога по теме нет (per-pack port-plan), поэтому relationship=unique; родственные IRLite-side файлы (другие *-port-plan, *-pipeline, patcher.md, sync-workflow.md, commit-checkpoints.md) портированы вместе и связаны через [[]]-ссылки, а не входят в канон-список redactor.
- **Связанные в redactor:** — (unique)
- Источник: портировано из памяти IRLite; оригинал оставлен как архив.
