---
name: iterationrp-reintegration-plan
description: "IterationRP re-integration TAKE 3 (started 2026-06-12) — the 2026-06-11 re-port FAILED (user verdict; the bugfix file's RESOLVED claims are wrong); fresh Modification copy made; donor = BSL lib internals + Photon runtime-nonTerrain shape + old-port call-site semantics; phases 1-5 (+6 optional) each in own session with an in-game sun/AE/TAA gate; verified pack facts (#version 430 core, LF index / CRLF worktree, anchors, no free colortex)"
metadata: 
  node_type: memory
  mod_scope: shader-inject
  ported_from: IRLite
  consolidated: 2026-06-18
  type: project
  originSessionId: 27e3eac9-cfd2-4bf4-bcbe-d59e1756d867
---

IterationRP re-integration TAKE 3. Parent: [[MEMORY]]. Failed-attempt archive: [[iterationrp-port-bugfix]]. Base-pack map (still valid for pristine): [[shader-iterationrp-pipeline]]. Patch format: [[patcher]]. Sync loop: [[sync-workflow]].

CONTEXT: the 2026-06-11 re-port FAILED (user verdict 2026-06-12) — NEITHER "RESOLVED" theory in [[iterationrp-port-bugfix]] (nested include / bounded-outline fix) is confirmed; that file is a lessons archive only. Its failed Modification copy was deleted; a FRESH copy Original->Shadres\Modification\IterationRP was made 2026-06-12 (pristine verified irlite-free first). CONTROL BASELINE: patches/iterationrp.irlpatch @ HEAD = the OLD June-06 generation (markers: 2x irlite_lightDiffuse separate loops; 0x irlite_lightSurface / IRLITE_VL_SHADOWS / textureGather; old fresnel outline) — its output + the then-current addon was verified working in-game 2026-06-11 (re-verify quickly at Phase 1 entry). Do NOT overwrite that patch until Phase 5 succeeds — it is the rollback.

VERIFIED PACK FACTS 2026-06-12 (overturn older memory where in conflict):
- composite25.fsh / composite51.fsh (and world1/world-1 twins) = `#version 430` -> SSBO + samplerCubeArray are CORE; NO #extension ops, NO wrapper edits (unlike Photon/BSL).
- EOL: git INDEX stores LF but the worktree is CRLF since the 2026-06-11 folder refactor (core.autocrlf=true); end-user packs are likely LF (match index). => new patch uses SINGLE-LINE ANCHORS ONLY; validation diffs need --ignore-cr-at-eol; author new irlite files as LF.
- Anchors verified once-each in pristine: Soild_FS.glsl:200 `#include "/Lib/BasicFunctions/Blocklight.glsl"`, :740 `color = color * gbuffer.albedo + sunlightSpecular;`; Volumetric_FS.glsl:52 the same include, :188 the `VolumetricFog(...)` call; Entities_FS.glsl:483 the Pack2xU8_to_U16 materialID line; shaders.properties:20 `iris.features.required=CUSTOM_IMAGES`.
- colortex budget: 10/11/12/16/17 sized by the pack's SR machinery, 6/7/8/9 (+10/11/12) resized under CUSTOM_RENDER_RESOLUTION -> NO obviously-free colortex; a reduced-res VL pass needs a colortex audit first (Phase 6, optional).
- Layout: top-level shaders/* = overworld; world1 / world-1 = thin includers of the same /Lib/Programs files -> Lib edits cover all dimensions. composite25 = Soild_FS, composite51 = Volumetric_FS, composite43 = SpecularVolumetric_FS (untouched).

DONOR DECISION (analysis 2026-06-12):
- Lib INTERNALS from Shadres/Modification/BSL/shaders/lib/irlite/irlite_lights.glsl (newest, 2026-06-12, ~53KB: merged irlite_lightSurface, degenerate-normal fallback, gather-PCF + texel floors + normal-offset, zero-beam-skip VL w/ steps 10 + shadow stride 2, bounded linear-ink INLINED outline, NaN guards). Cross-refs: Complementary lib (same gen), Photon lib (deferred call shapes).
- nonTerrain = RUNTIME function param (Photon shape), NOT compile-time IRLITE_NONTERRAIN — IterationRP's entity flag is bit-7 of the materialID byte (Entities_FS +128, decoded in Soild_FS); one deferred shading pass for everything.
- Call-site SEMANTICS = the old working port ([[shader-iterationrp-pipeline]]): diffuse added to `color` PRE-albedo-multiply; spec POST-albedo additive; reuse the pack's SpecularGGX(n,v,l,roughness,f0) with REAL gbuffer roughness/metalness (material-coupled spec everywhere — BSL's nonTerrain-only spec rule NOT needed, IterationRP has real PBR); spaces camera-RELATIVE (worldPos; absolute = +cameraPosition).

HARD RULES (lessons that stand regardless of the unconfirmed diagnosis):
- NO nested includes inside irlite files; never start a comment line with `#include`.
- EVERY additive term BOUNDED + isnan/isinf-sanitized: Exposure_CS = unclamped arithmetic tile-mean AE — one unbounded/NaN HDR pixel craters global exposure; IRC/PT denoiser + corner-pixel scalar relays STICK NaNs.
- Toggle hygiene: every feature behind its own #define so an in-game ladder can bisect any regression (the phase gates rely on it).

PHASES (each in its OWN session; gate = user verifies in-game: the feature works AND sun/AE/TAA stay intact; sync = copy Modification\IterationRP -> run\shaderpacks\IterationRP_IRLite + Iris hot reload; commit checkpoint on user confirm per [[commit-checkpoints]]):
- Phase 0 — DONE 2026-06-12: donor analysis, pack-fact verification, fresh Modification copy, this plan.
- Phase 1 — core surface light: DONE 2026-06-12 — gate PASSED in-game, committed bc91702 (566 files = the fresh Modification pack copy + the 4-file delta; NOTE: repo .gitignore line 123 has a global `shaders/` rule — any NEW file under a Modification shaders folder needs `git add -f`, already-tracked files are unaffected). Delta = exactly 4 files (validated by --ignore-cr-at-eol diff vs pristine):
  - +shaders/Lib/irlite_lights.glsl (172 lines, LF): options (DIFFUSE/SPECULAR/INTENSITY/SPECULAR_INTENSITY/TOON*), SSBO struct (donor copy), surface half under `#ifdef IRLITE_SURFACE_PASS` = merged irlite_lightSurface(worldPos CAMERA-RELATIVE, normal, viewDirW, roughness, f0, nonTerrain, out diffuse, out spec) — Photon loop internals (dist2 cheap-reject, Frostbite window, cone, toon) + pack SpecularGGX world-space + 5x baselines + isnan/isinf output sanitize. NO shadow/VL/outline code in the file at all (Phase-2/3 comments mark the insert points).
  - Soild_FS: include op after the Blocklight.glsl include (`#define IRLITE_SURFACE_PASS` + include), one block around the `color = color * gbuffer.albedo + sunlightSpecular;` anchor: bit-7 decode (texelFetch(FBTEX_GSOLID_DATA).z*65535 &128u — verbatim old patch), merged call, diffuse += pre-albedo, spec += post-albedo (each behind its own #ifdef).
  - Entities_FS: `(v_materialIDs + 128.0) / 255.0` replace (old patch op verbatim).
  - shaders.properties: `iris.features.required=CUSTOM_IMAGES SSBO`.
  Verified safety facts: Unpack2xU8_ID_from_U16 masks `& 127u` (Utilities.glsl:182) -> pack materialID compares unaffected by bit-7; MATID max = 111 (Settings.glsl) -> +128 <= 239, no byte overflow (Pack2xU8_to_U16 saturates anyway); the only bit-7 reader on the solid channel = Composite_Copy_CS under `#ifdef LOD_RENDERING` (entity pixels flip its copySolid heuristic when that pack option is ON — known benign interaction, old port shipped it). NETHER branch NOT hooked (old-port parity): Soild_FS `#ifndef DIMENSION_NETHER` :599 has an `#else` whose final line `color = (color * (1.0 - metalnessMask * METALMASK_STRENGTH) + textureLighting) * gbuffer.albedo;` (~:760) is the anchor IF the user asks for nether lights later (diffuse before it, spec after it, no sunlightSpecular var there). Synced to run/shaderpacks/IterationRP_IRLite (full copy).
- Phase 2 — shadows: DONE 2026-06-12 — gate PASSED in-game (incl. the bobbing fix below), committed 3757232. Lib-only delta (irlite_lights.glsl): the shadow block ported VERBATIM from the Photon Modification lib (irl_ samplers, PCSS quality tiers, vogel/distFromDepth01/rotationPhi/depthBias helpers, irlite_spotGatherTap, irlite_spotShadow, irlite_pointShadow — gather-PCF + texel floors + normal-offset included; BSL lib cross-checked: its shadow funcs are byte-identical to Photon's, all its other deltas are forward-pack-specific) + ONE functional addition: outerDeg clamped to [1, 179] deg in irlite_spotShadow so fY = 1/tan(outer/2) stays finite (the wide-spot NaN path from the take-2 post-mortem). Block gated `#if defined IRLITE_SHADOWS && defined IRLITE_SURFACE_PASS` -> IRLITE_COMPILE_SHADOWS (Phase 3 extends the gate with the VL_SHADOWS term); options added (IRLITE_SHADOWS, QUALITY 2, SIZE 0.10, BIAS 0.05, NORMAL_OFFSET 0.05); the merged loop got the ONE shared visibility multiply + `if (attenuation <= 0.0) continue;`. samplerCubeArray core at 430, no extension. Gate: spot+point shadows correct (no acne / no peter-panning), PCSS softness grows with occluder distance, per-light Shadows toggle (addon sets vlParams.w=-1) works, WIDE spot (>=90 deg outer) shows no NaN/black-frame, sun/AE/TAA stable.
- IN-PHASE BUGFIX 2026-06-12 (found at the Phase 2 gate, fix VERIFIED in-game): VIEW-BOBBING WOBBLE — light pattern swims with MC view bobbing ON, IterationRP-only. Root cause: Soild_FS:540 `worldPos = mat3(gbufferModelViewInverse) * viewPos` is ROTATION-ONLY; the modelview TRANSLATION column (= the bobbing offset, zero with bobbing off) is dropped, so fragWorld oscillated. Fix = pass `worldPos + gbufferModelViewInverse[3].xyz` into irlite_lightSurface — the pack's OWN idiom (voxelPos :579, ShadowTracing :670). `normalize(-worldPos)` as frag->eye stays correct WITHOUT the add (eye = MVI[3].xyz in player space, offset cancels in the difference). The OLD port had the same latent bug (films use locked cameras — never noticed). Other ports unaffected (full-mat4 position reconstruction).
- Phase 3 — volumetric: DONE 2026-06-12 — BOTH ladder steps gate-PASSED in-game (step 2 = IRLITE_VL_SHADOWS ON, irl_* samplers active in composite51 — the take-2 prime suspect is EXONERATED in take-3: pack stays stable with them active). Committed as the phase checkpoint right after the step-2 confirm. Implementation: VL half ported VERBATIM from the BSL Modification lib (zero-beam `dot(lightCol,lightCol)<1e-8` skip BEFORE clip math, hoisted per-step absorption, strided shadow taps w/ cached visibility, capped shFY hoist) under nested bare `#ifdef IRLITE_VL_PASS` + `#ifdef IRLITE_VOLUMETRIC` (Iris bool-registration rule), + isnan/isinf sanitize on the result; ABSOLUTE-world signature irlite_volumetric(startWorld, endWorld, worldDir, dither). COMPILE_SHADOWS gate extended with `(IRLITE_VL_PASS && IRLITE_VOLUMETRIC && IRLITE_VL_SHADOWS)` -> with VL shadows off composite51 declares NO irl_ samplers (only the SSBO). Volumetric_FS 2 ops: include after Blocklight.glsl (`#define IRLITE_VL_PASS` first); call after the VolumetricFog line — INSIDE `#ifdef VFOG` (old-port parity: IRLite VL is off when the pack's VFOG option is off — accepted; rayWorldPos itself is computed outside the gate at :145-153 if relocation is ever wanted). BOBBING handled: Volumetric_FS:123-125 worldPos/worldDir are mat3-only — worldDir is correct as-is (offset cancels), but the march origin = bobbed eye -> call passes irliteVLStart = cameraPosition + gbufferModelViewInverse[3].xyz, endWorld = start + rayWorldPos. Dither = the old port's static spatial hash (no temporal dep). Defaults: VL_STEPS 10, VL_SHADOW_STRIDE 2, VL_INTENSITY 1.0, MAX_DIST 96. Gate step 1: spot cone beams + point haze render and follow lights, beamStrength=0 light emits no VL, no wobble with bobbing, pack VFOG interplay sane, fps acceptable (full-res march — the reduced-res pass stays optional Phase 6), sun/AE/TAA stable.
- Phase 4.5 — outline: REGRESSED AFTER THE SETTINGS PHASE — the take-2 pattern RECURRED. Timeline matters: the 4.5 gate (outline ON via hand-edited #define, NO settings entries yet) PASSED including the AE stress test, committed 30af060; but after Phase-4 settings landed, the pack broke GLOBALLY with outline merely PRESENT even when toggled OFF (user 2026-06-12). KEY DIAGNOSTIC DELTA vs the passed gate: the outline options entered Iris's option system (screens/sliders/lang) and the toggle was flipped through the UI (Iris rewrites #define lines) — vs hand-edited defines before. BISECT STEP 1 (2026-06-12): outline GLSL fully stripped (option defines + detector/ink section + Soild_FS call site; breadcrumb comments left; full code preserved at 30af060), settings refs in properties/lang DELIBERATELY left in (expect benign Iris unknown-option menu warnings) -> STEP-1 VERDICT (user, 2026-06-12): RENDERING RESTORED with the GLSL stripped and settings refs still present — THE OUTLINE GLSL BLOCK ITSELF IS THE TRIGGER, settings/option-system exonerated. take-2's "strip renders correctly" bisect is thereby REPRODUCED on a fully rewritten block (no nested include, bounded ink, standalone loop — none of that mattered). DECISION (plan fallback): IterationRP ships WITHOUT outline; all IRLITE_OUTLINE* entries also removed from shaders.properties + lang (cleanup after the bisect); the full bounded implementation survives at commit 30af060. EXONERATED by the OLD working fresnel outline (June-06 patch had: option named IRLITE_OUTLINE, bare #ifdef gating of CALLS, ungated irlite_outlineFactor function, depthtex0 + LinearDepth_From_ScreenDepth taps — all fine in production): option names, bare-#ifdef registration, depth taps. STRUCTURAL DELTAS of both BROKEN blocks (take-2 Photon-style detector AND take-3 rewrite) vs the old WORKING one: local-array constructors in the detector (`ivec2 offs[8] = ivec2[8](...)`, `float z[8]`) + the large gated block shape; the old detector used 4 discrete taps, no arrays. IF outline is ever re-attempted on IterationRP: experiment = ARRAY-FREE detector written in the old outlineFactor shape (discrete taps, function defined unconditionally, only calls gated) — the one untested structural hypothesis; test with toggle OFF first (the breakage manifests even compiled-out, so whatever Iris does to the raw file text is implicated). Host = Soild_FS post-albedo (models confirmed in the SOLID gbuffer — phases 1-3 light them; composite25 depth is complete). Architecture = STANDALONE irlite_outlineInk() loop (BSL/Complementary style — the verified irlite_lightSurface untouched), gated bare `#ifdef IRLITE_OUTLINE` inside the surface half. Detector: silhouette Laplacian on depthtex0 via pack LinearDepth_From_ScreenDepth (abs + 65536 clamp; depthtex0 declared by pack UniformDeclare.glsl — NOT redeclared) + border via bit-7 texelFetch of FBTEX_GSOLID_DATA; crease NOT ported; flat normal = gbuffer.vertexNormal (real per-face — no derivative reconstruction). BOUNDED INK (the take-2 killer, rebuilt): hue = lightCol/max(peak,1e-4) with intensity EXCLUDED, NO x5, `ink = min(ink, IRLITE_OUTLINE_MAX)` (default 2.0), NaN sanitize, call site adds WITHOUT IRLITE_INTENSITY; PCSS shadow taps on rim pixels after cheap frontKill/backKill rejects; lazy edgeMask + break-on-zero. Call args = bobbing-corrected relPos (worldPos+MVI[3].xyz), gbuffer.vertexNormal, normalize(-worldPos), irlite_nonTerrain. `#define IRLITE_OUTLINE` is ON in Modification FOR THE GATE — PHASE-5 REMINDER: flip to `//#define IRLITE_OUTLINE` (ship default OFF) before generating the patch. Gate: lines on lit silhouettes (rim logic: backlight inks, frontal doesn't), TARGET=1 entities-only, border at feet/wall contact, AND THE AE TEST — bright lights + silhouettes near the sun in frame, exposure must stay stable (if AE craters again -> ship with outline OFF/absent and close).
- Phase 4 — settings (NOW AFTER 4.5): IMPLEMENTED 2026-06-12, AWAITING the gate. shaders.properties 3 ops (anchors verified unique): replace the `		[SHORTCUT]          [MOD_SUPPORT] \` line (:833, main screen entry [IRLITE] + <empty> above it), after `screen.LIGHTING=[LIGHT_SOURCE] [HELDLIGHT] [SHADOW]` (:879, the IRLITE screen tree — 6 sub-screens, columns=2), after `sliders=PT_VOXEL_RESOLUTION \` (:1189, 19 numeric IRLITE_* sliders — TARGET deliberately off-list so it renders as a value cycle). lang/en_us.lang 1 op after `option.iterationRP_VERSION=...` (:3): colour-coded labels (old patch scheme) + .comment lines for the non-obvious options (NORMAL_OFFSET, VL_SHADOWS, STRIDE, WRAP, OUTLINE_MAX) + value.IRLITE_OUTLINE_TARGET.0/1. Deltas vs the OLD patch's settings: + IRLITE_SHADOW_NORMAL_OFFSET, IRLITE_VL_SHADOWS, IRLITE_VL_SHADOW_STRIDE, IRLITE_OUTLINE_TARGET/WRAP/DEPTH_THRESHOLD/MAX; − IRLITE_SPECULAR_HARD (obsolete with the merged loop), − IRLITE_OUTLINE_FRESNEL_POWER (old fresnel outline gone). All bool toggles verified to have bare #ifdef registration in GLSL. After the outline bisect resolved (GLSL block = culprit, settings exonerated), ALL IRLITE_OUTLINE* entries were removed from properties/lang too; the outline-free settings UI gate PASSED and Phase 4 was committed 9b0060b (settings + the full outline excision in one checkpoint). Final settings surface: 6 toggles, 14 sliders, 4 sub-screens (Specular/Volumetric/Shadows/Toon).
- Phase 5 — DONE 2026-06-12: patches/iterationrp.irlpatch REGENERATED new-gen (12 ops: +file lib; Soild_FS after-include/before-anchor/after-anchor; Volumetric_FS after-include/after-VolumetricFog; Entities_FS replace; properties features-replace/[SHORTCUT]-replace/screen-tree-after/sliders-after; lang after) by tools/gen-iterationrp-patch.ps1 (body-splice from Modification + boundary asserts; before-op anchors carry \t escapes for byte-exact output; [SHORTCUT] and sliders anchors escape their backslash; screen-tree end boundary is CONTENT-defined — the toon tail line — because pristine has a blank right after the screen.LIGHTING anchor and a pristine-next-line boundary grabs the inserted blank instead). Validated: PatchHarness (4 pure patcher classes + harness, javac) applied to pristine -> git diff --no-index --ignore-cr-at-eol vs Modification EMPTY. PORT COMPLETE (no outline).
- Phase 6 — OPTIONAL (on user demand, after the port is verified): reduced-res VL pass (BSL deferred2 recipe) — requires the free-colortex audit vs the pack's SR/CUSTOM_RES machinery.

OPEN VERIFICATION ITEMS: (1) confirm BBS replay models actually receive Soild_FS light in IterationRP today (historically yes — the old port is the user's production tool); (2) outline host / depth-source choice (Phase 4.5); (3) whether to keep the old fresnel outline as a cheap alternative toggle (it shipped working in the old patch).

---

## Связь с IRL-redactor (объединение памяти 2026-06-18)

- **Применимость:** shader-inject — Применимо к IRLite (источник правды .irlights-патчей; авторинг в Shadres/Modification → односторонний синк в redactor через copy-patches.ps1). Дополняет project-port-1211: там iterationrp лишь упомянут как «не установлен, ожидается 12 ops» в офлайн-проверке — здесь полное содержание интеграции пака и сам patch.
- **Связанные в redactor:** [[project-port-1211]] (complements)
- Источник: портировано из памяти IRLite; оригинал оставлен как архив.
