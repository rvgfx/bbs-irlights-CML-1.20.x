---
name: project-port-1214-overnight
description: "Автономный ночной порт redactor на MC 1.21.4 (redactor-only) — фоновый воркфлоу запущен 2026-06-18, утром ревью"
metadata: 
  node_type: memory
  mod_scope: redactor-only
  type: project
  originSessionId: 7e16176e-0f84-40e3-b103-c69886f7aa21
---

✅ **ЗАВЕРШЁН ЗЕЛЁНЫМ 2026-06-18.** redactor на MC 1.21.4 собирается: `./gradlew build -x test` SUCCESSFUL, jar `irl-redactor-1.0.0.jar` (~7.5 МБ). Ветка `port/1.21.4` = 3 коммита: `bd1f5b1` scaffold+деп-матрица → `137db43` фикс client render-API (13 сломов 1.21.2→1.21.4 в 4 файлах, +25/−32, без заглушек) → `8869337` NOTES. Дерево чистое, НЕ запушено.

⚠️ **Воркфлоу `wf_083aecd4-ca8` УМЕР на лету** (fix-агент применил правки и скомпилил зелено, но был оборван до коммита — вероятно фоновый ран порвали прерывания мейн-сессии). Восстановлено вручную из рабочего дерева: форс-рекомпиляция (`--rerun-tasks`) + полная сборка подтвердили зелёность → закоммичено. **Урок: фоновый Workflow хрупок к прерываниям/компакту мейн-сессии — для длинных автономных прогонов держать heartbeat-цикл (ScheduleWakeup) И проверять git-состояние, а не только ждать task-notification.**

**Правки (137db43, эталон был port/1.21.11 пост-рерайт):** `GameRenderer::getPosition[Color]Program`→`ShaderProgramKeys.POSITION[_COLOR]`; `setProjectionMatrix` VertexSorter→`ProjectionType.PERSPECTIVE`; `applyModelViewMatrix()` убран (стек живой); `getVertexSorting()`→`getProjectionType()`; `VertexBuffer.Usage.STATIC`→`GlUsage.STATIC_WRITE`; `getCullingShape(world,pos)`→`getCullingShape()` (1.21.2); `EntityRenderDispatcher.render()` убран лишний yaw-арг (1.21.2 EntityRenderState).

**✅ runClient boot-test ПРОЙДЕН + порт подтверждён пользователем (2026-06-18, через computer-use):** dev-клиент бутится на 1.21.4 до титула, грузит свежий Creative-мир, **рендерит с Iris-шейдерами без краша** → render-path правки (ProjectionType / убранный applyModelViewMatrix в ShadowRenderer) базовый рендер не сломали. ⚠️ Остаётся НЕ проверенным глазами сам ImGui light-editor + бейк теней (Escape/бинд ловил фокус окна; юзер объявил успех и закрыл раньше открытия GUI) — если будут странности именно в редакторе/тенях, копать там. Существующие тест-миры юзера = версии 1.21.11 (новее) → в 1.21.4 не открывать (даунгрейд-коррапт); тестил на новом мире. Подробности в `NOTES-1.21.4.md`.

---
(исходный план запуска ниже, для истории)

🌙 АВТОНОМНЫЙ НОЧНОЙ ПОРТ запущен 2026-06-18 (ultracode, фоновый Workflow `wf_083aecd4-ca8`, имя `port-redactor-1214`). Пользователь спит, ревью утром.

**Объём (сузили с исходной заявки):** ТОЛЬКО redactor на **MC 1.21.4**. Аддон ОТБРОШЕН (BBS-эталон bbs-fs есть только до 1.21.1, `bbs-2.2.1-1.21.1.jar` — максимум → трилогию на 1.21.4 не собрать). Версия **26.1 ОТЛОЖЕНА** (неясная/возможно снапшот с сырыми маппингами — пользователь выбрал «пропустить, сделать только 1.21.4»). irl-core НЕ трогаем (общее ядро, plain-Java, версионно-нейтрально).

**Стратегия:** новая ветка `port/1.21.4` форк от **`port/1.21.1`** (база до рерайта рендера 1.21.5/1.21.9; дельта 1.21.1→1.21.4 маленькая). Планка приёмки = **best-effort до зелёной `./gradlew build -x test` + `NOTES-1.21.4.md`**. Локальные коммиты разрешены (юзер временно снял [[commit-checkpoints]] и [[feedback-no-per-session-branch]] для этой задачи), **пуш ЗАПРЕЩЁН**.

**Деп-матрица 1.21.4 (ПРОВЕРЕНА по FabricMeta/Modrinth):**
- minecraft_version=1.21.4 · yarn=1.21.4+build.8 · loader=0.16.14 (без изм.) · fabric=0.119.4+1.21.4
- iris=`1.8.8+1.21.4-fabric` ← ТА ЖЕ 1.8.8-линия что 1.21.1 → 2-арг `addDynamicSampler`, сэмплер-миксины НЕ трогать
- sodium=`mc1.21.4-0.6.13-fabric`
- **replaymod УДАЛЁН** из дев-сборки (по прямой просьбе юзера; modRuntimeOnly → compile-safe, совместимость в редакторе через рефлексию, не через replaymod-классы)

**Ожидаемые поломки:** yarn-переименования 1.21.1→1.21.4 + возможный `EntityRenderState` (введён 1.21.2, если есть энтити-рендер в тенях). Input (Mouse/Keyboard) на 1.21.4 скорее всего без изменений (record-обёртки пришли позже, на 1.21.11).

**Воркфлоу:** Scaffold(ветка+матрица+1й билд) → Fix(итер. build-fix цикл ≤16 раундов, стоп при зелёной ИЛИ 3 раунда без прогресса) → Report(NOTES + финальный коммит). Скрипт: `…/workflows/scripts/port-redactor-1214-wf_083aecd4-ca8.js`. Resume: `Workflow({scriptPath, resumeFromRunId:"wf_083aecd4-ca8"})`.

**Утром проверить:** `git -C …/irlights checkout port/1.21.4`, читать `NOTES-1.21.4.md`, `git log --oneline port/1.21.1..port/1.21.4`. Если зелено — кандидат на runClient boot-test (не входил в ночную приёмку).

---

## ОЧЕРЕДЬ: порты 26.x (НЕ запущены, ждут подтверждения юзера)

⚠️ **Minecraft перешёл на новую схему версий «26.x»** (проверено FabricMeta 2026-06-18). Выпущенные СТАБИЛЬНЫЕ: **26.2** (новейшая), 26.1.2, 26.1.1, 26.1. **26.3 и 26.4 НЕ СУЩЕСТВУЮТ** (ещё не вышли — портировать не во что).

- Юзер просил «26.1» (отложена) и «26.4» (новая просьба). **26.4 = несуществующая версия** → либо имелась в виду 26.2, либо это будущая цель «когда выйдет». 26.1 — реальна (26.1.2 последний патч), я зря раньше счёл её сырым снапшотом.
- **Тулчейн под 26.2 ЕСТЬ:** iris `1.11.1+26.2-fabric`, sodium `mc26.2-0.9.0-fabric`. (yarn 26.2/26.1.2 — meta-эндпоинт флакал, перепроверить.)
- ⚠️ **26.x порт = БОЛЬШОЙ, НЕ как 1.21.4.** База = форк от **`port/1.21.11`** (после рерайтов 1.21.5/1.21.9, эра 26.x ещё дальше). iris прыгнул **1.8→1.11** → API сэмплеров ТОЧНО другой (4-арг `addDynamicSampler` с TextureType, как уже на port/1.21.11, НЕ 2-арг). sodium 0.6→0.9. Это не «поставил и забыл» — нужен явный go юзера + точная версия (26.2? ждать 26.4?) + подтверждение базы.

**Решить с юзером утром:** (1) 26.4 не существует — он про 26.2 или ждём релиз 26.4? (2) делаем 26.1.2 и/или 26.2? (3) подтвердить базу = port/1.21.11.

Связь с общей стратегией [[project-irl-sync-strategy]], форк-карта [[reference-edit-routing-by-area]].
