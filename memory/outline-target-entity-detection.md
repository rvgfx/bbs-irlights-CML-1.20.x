---
name: outline-target-entity-detection
description: "IRLITE_OUTLINE_TARGET [0 1 2] (0 both/1 entities/2 blocks, как lightMask) — per-pack способ отличить entity от block в outline composite-проходе. Done+runtime+committed: Complementary, Solas, BSL, RethinkingVoxels, Bliss. Photon done+runtime-verified in-game 2026-07-01 (НЕ committed). Pending: IterationRP."
metadata: 
  node_type: memory
  mod_scope: shader-inject
  type: reference
  originSessionId: cfb2571c-017c-43bb-a68a-72acf0fe54bb
---

Фича 2026-06-30: к старому outline ([[project-photon-outline-switch-to-old]]) добавлена опция IRLITE_OUTLINE_TARGET // [0 1 2] (0 both / 1 only entities / 2 only blocks) — те же коды, что lightMask в SSBO (0 all/1 entities/2 blocks). Гейт ставится в composite-проходе, где зовётся irlite_outlineInk: outline рисуется только если пиксель проходит target-фильтр по per-pixel entity-метке из gbuffer пака. Паттерн гейта (compile-time, не входим в outline зря): bool irliteODraw=<z-gate>; #if IRLITE_OUTLINE_TARGET!=0 { читаем метку; #if==1 draw=isEntity #else draw=!isEntity } #endif; if(irliteODraw){...outline...}. GUI: IRLITE_OUTLINE_TARGET в screen.IRLITE_OUTLINE_SETTINGS сразу за IRLITE_OUTLINE + в sliders= (в середину, хвост остаётся IRLITE_OUTLINE_GLOW_STRENGTH — иначе падают slBody.EndsWith-ассерты генераторов) + lang option/comment/value.0-2 (Solas: en_US+ru_RU).

Ключ: BBS-морфы (актёры) рендерятся entity-путём и получают ДЕФОЛТНУЮ entity-метку пака (НЕ PBR-специфичную) — детектор обязан ловить дефолт, иначе морфы не распознаются (симптом: Only Entities пусто, Only Blocks рисует и на entity). Это была первая ошибка на Complementary (ловил только PBR 100..199).

Per-pack entity-метка (в composite-проходе):
- Complementary (done): materialMask = colortex6.g*255. entity = (100..199) ИЛИ ==254 (дефолт entity/hand, сюда же BBS-морфы); block/terrain дефолт 0. gbuffers_entities:143 дефолт 254, +100 только при PBR (!=254). colortex6 пишется всегда, не перезаписывается до composite1. Тест: irliteOMat>99.5 && <199.5 || >253.5.
- Solas (done): РОДНАЯ метка — gbuffers_entities пишет colortex3=(0,0,0,1) безусловно (нормали закомментированы), terrain пишет encodeNormal(n)=n.xy/f+0.5 (.xy≳0.15, центр 0.5). colortex3=RGBA16, не перезаписан до composite1, уже объявлен. Тест: colortex3.xy.x<0.1 && .y<0.1 = entity. Без инжекта.
- BSL (done, ИНЖЕКТ): нет готовой метки в стоке (colortex1=RGB8 без alpha; родной colortex3.z=entityMask пишется лишь при TAA_SELECTIVE/ADVANCED_MATERIALS — оба off по умолчанию; hand=z0<0.56 чисто depth). Инжект: gbuffers_entities base DRAWBUFFERS:01->013 + gl_FragData[2]=vec4(0,0,entityMask,1) (entityMask=0.25 для непрозрачных, стр ~182); composite +uniform sampler2D colortex3 + читаем .z>0.1. terrain НЕ трогаем (colortex3 clear=0 -> не-entity). Под-ветки MCBL/PBR re-pin свой layout (последний DRAWBUFFERS выигрывает — паттерн пака). Caveat: конфиг MCBL_SS-only (без TAA/ADV) метку не пишет -> target не сработает (MCBL off по умолчанию, приемлемо). gen-bsl-patch.ps1 ОБНОВЛЁН: +2 операции (composite after "uniform sampler2D colortex1;"; gbuffers_entities replace "/* DRAWBUFFERS:01 */"->013 + after FragData[1]).
- Bliss (done+committed 5ab047c): канал = native opaqueMasks из colortex1.a (irlite_decodeVec2Y) ПРЯМО в outline-блоке composite3.fsh (там уже исключается hand 0.75). Метки opaqueMasks (composite1.fsh:848-866, родной классификатор): 0.45 entity / 0.50 lightning / 0.55 leaf+air-waving / 0.60 grass / 0.75 hand / 1.0 terrain. ГОЧА: native 0.45 УСЛОВНА — all_solid.vsh:252 ставит её только для entityId ∈ {ENTITY_PLAYER 1601, SSS_MEDIUM 1801, SSS_WEAK 1802, 2468 (родное число Bliss)}; дефолт любой entity (BBS-морфы-актёры) = normalMat.a 1.0 = неотличим от террейна (выяснено: entity.properties маппит лишь entity.1601=player, морфы НЕ player-путём). РЕШЕНИЕ (выбор юзера «все entity → 0.45»): безусловный инжект all_solid.vsh после L252 `if(normalMat.a > 0.99) normalMat.a = 0.45;` (ловит дефолт-entity; lightning 0.50 перезаписывает позже). Гейт composite3: `bool irliteODraw=!(abs(opq-0.75)<0.01); #if TARGET!=0 { isEntity=abs(opq-0.45)<0.01; ==1 draw&&=isEntity #else draw&&=!isEntity }`. Side-effect: морфы теряют родной sun-SSS (composite1 sunSSS_density=0 при метке 0.45) — мелко, юзер ок. gen-bliss-patch.ps1 +1 op (@file all_solid.vsh, after anchor L252, body=fallback-строка splice из Modification); round-trip 17 ops байт-в-байт. lang en_us+ru_RU (Bliss несёт ru). GUI: Bliss = свой screen.IRLITE_OUTLINE_SCREEN (НЕ Solas-формат _SETTINGS с <empty>).
- RethinkingVoxels (done+committed 0f14cd3): Complementary-форк — тот же materialMask (colortex6.g RGBA8, gbuffers_entities дефолт 254 +100 PBR), гейт в program/composite.glsl (нет composite1), texelFetch(colortex6,texelCoord,0).g; gen-rethinkingvoxels не менялся (cOutline берёт весь блок). VL=colortex15 ([[rethinkingvoxels-pipeline]]).
- Photon (done 2026-07-01, artifacts+round-trip valid + RUNTIME-VERIFIED in-game photon_v1.3b_IRLights: пайплайн скомпилирован чисто, гейт работает; НЕ committed): САМЫЙ ПРОСТОЙ — метка УЖЕ в scope. Photon outline живёт INLINE в irlite_lights.glsl::irlite_lightSurface (не в отд. composite-проходе), а эта функция УЖЕ принимает bool nonTerrain (= material_mask&128u bit, ставится в gbuffers_all_solid при PROGRAM_GBUFFERS_ENTITIES/BLOCK/HAND, декод+&127 в d4) и УЖЕ юзает её для per-light lightMask. Гейт = compile-time в per-fragment outline-блоке функции: `bool irliteODraw=true; #if TARGET==1 irliteODraw=nonTerrain #elif==2 irliteODraw=!nonTerrain #endif; float contourFactor=0.0; if(irliteODraw){ irlFres=...; contourFactor=irlite_outlineFactor()*irlFres; +GLOW }`. Фильтр обнуляет contourFactor => per-light `outlineOut += lightCol*(contourFactor*...)` = 0, И пропускает дорогой 5-tap irlite_outlineFactor() для отфильтрованных пикселей. НЕ требует инжекта метки (в отличие от BSL/Bliss). BBS-морфы = entity-путь -> бит 128 -> ловятся. КОУПЛИНГ: правки в ОБА патча (bbs-irlights-addon/patches/photon.irlights 19 ops + bbs-dof-addon/patches/photon-irl-dof.irlights 36 ops); генератора НЕТ (photon руками — [[reference-edit-routing-by-area]]). Round-trip PatchHarness (tools/build, старый пакет qualet.irlite.client.patcher, семантика CONTRACT_VERSION=1) на Shadres/Original/Photon: irlite_lights.glsl content==Modification EOL-agnostic (Modification CRLF vs патчер LF — ожид.; --strip-trailing-cr чисто), DoF-слой (irl_dof_focus.glsl/c2_dof) цел. GUI: TARGET за IRLITE_OUTLINE в screen.irlite_outline (плоский список, НЕ Solas _SETTINGS-формат) + перед PIXEL_SIZE в sliders (хвост GLOW_STRENGTH). lang ТОЛЬКО en_US (Photon несёт 8 языков, но IRLite-опции лишь в en_US -> прочие фолбэк на англ). Photon в BbsPatcherHost.BUNDLED (стр 29).

ГОЧА BUNDLED (фикс в 0f14cd3): новый пак, добавленный в patches/, ДОЛЖЕН быть в hardcoded списке BbsPatcherHost.BUNDLED (src/client/.../patcher/BbsPatcherHost.java) — иначе in-game patcher НЕ рефрешит его рабочую копию в run/irlite/patches/ из jar и применяет stale ручную копию (симптом: новые опции/кнопки не появляются в Iris после Apply Patch, хотя .irlights в jar правильный). RethinkingVoxels этим страдал (был в patches/ + jar, но не в BUNDLED — 6 паков). Список теперь 7 (+rethinkingvoxels). При портировании нового пака проверять BUNDLED. Диагностика: grep TARGET в run/shaderpacks/<pack>_IRLights (applied) vs patches/<pack>.irlights vs jar — расхождение = stale working copy.

ГОЧА STALE-WORKING-COPY-2 (Bliss-сессия 2026-07-01, диагностирована, НЕ пофикшена): даже когда пак В BUNDLED, рабочая копия run/irlite/patches/<pack>.irlights может НЕ рефрешнуться из свежего bundled -> in-game Apply Patch применяет stale (симптом: новой опции нет в Iris; лог `(irlite) [patch] Patched OK -> X (16 ops)` вместо 17). Корень: PatchLibrary.extracted (irl-core, [[patcher]]) = static volatile, рефреш bundled «runs at most once per session» ГЛОБАЛЬНО (не per-host); в dev оба мода (irlite + irl_dof) делят ОДИН irl-core -> кто первый дёрнул PatchLibrary.dir()->extractBundled, тот зафиксил extracted=true, второй пропущен (НЕТ лога 'Refreshed/Unpacked <pack>'; irl_dof рефрешит свои *-irl-dof, irlite свои bliss/bsl/... — один проигрывает). Обход вручную: cp patches/<pack>.irlights -> run/irlite/patches/ + java -cp tools/build PatchHarness patches/<pack>.irlights run/shaderpacks/<pack>(pristine) run/shaderpacks/<pack>_IRLights. ПОДТВЕРЖДЕНО на Photon 2026-07-01: рантайм bbs-irlights-addon ([[feedback-addon-runclient-command]]) — jar/build.resources свежий (TARGET=10, processResources отработал не UP-TO-DATE), но run/irlite/patches/photon.irlights завис на пред. итерации (TARGET=0, BACK=6), in-game Apply Patch собрал applied photon_v1.3b_IRLights БЕЗ опции (лог: irl_dof «Installed DOF patch» первым -> PatchLibrary.extracted=true -> irlite рефреш пропущен). Рецепт обхода для zip-пристина: cp patches/photon.irlights run/irlite/patches/ ; unzip run/shaderpacks/photon_v1.3b.zip -> tmp ; PatchHarness patches/photon.irlights tmp run/shaderpacks/photon_v1.3b_IRLights (перезапись, 19 ops, .txt-настройки Iris рядом целы; Iris не залочил пак пока активен другой). После — в игре выбрать пак + reload шейдеров. Durable-фикс (open): PatchLibrary.extracted -> per-host Set<host>/Map. runClient грузит мод из build/classes+build/resources (sourceSet), НЕ из dev jar; processResources кэширует patches/ (нужен --rerun-tasks); финальный jar = remapJar (build/libs), не jar task.

Цикл на каждый пак: правки в Shadres/Modification/<pack> -> gen-<pack>-patch.ps1 -> PatchHarness apply на Original + diff --ignore-cr-at-eol == Modification (0) -> gradlew remapJar --rerun-tasks (НЕ jar — финальный jar пишет remapJar; processResources кэшит patches/, нужен --rerun-tasks) -> copy build/libs/irlite-1.1.jar в C:\prismlauncher\instances\BBS\minecraft\mods\irlite-1.20.x.jar -> юзер Apply Patch в игре. Все правки дублировать в DoF-комбо bbs-dof-addon ([[project-photon-outline-switch-to-old]] DoF-коуплинг) — задача отдельная. Photon done 2026-07-01, осталась отдельная сессия IterationRP. См. [[sync-workflow]], [[shader-settings]].

ГОЧА REDACTOR-SYNC СЛОМАН (найдено 2026-07-01, Photon-сессия, НЕ пофикшено): скрипт irlights/tools/copy-patches.ps1 (упомянут в [[reference-edit-routing-by-area]] как механизм одно-направл. синка .irlights IRLite->redactor) ФИЗИЧЕСКИ ОТСУТСТВУЕТ (find по всему BBS, вкл. worktrees = 0). Как следствие redactor-копии .irlights глубоко дрейфуют: irlights/src/client/resources/assets/irl-redactor/patches/photon.irlights последний раз тронут коммитом 7d62612 (порт cookie) — ДО switch-to-old, всё ещё несёт СТАРЫЙ Blender-style outline (отд. файл irlite_outline.glsl + WRAP/DEPTH_THRESHOLD/CREASE/CREASE_ANGLE + старый TARGET [0 1]), БЕЗ front/back/glow, БЕЗ моего TARGET[0 1 2]. Т.е. addon (owner, [[project-photon-outline-switch-to-old]]) ушёл на неск. итераций вперёд, redactor завис. Ресинк redactor = отдельная задача (полная замена photon.irlights свежим из addon, не только TARGET) + сперва восстановить/переписать copy-patches.ps1. НЕ трогалось в этой сессии (out of scope).
