---
name: project-photon-outline-switch-to-old
description: "Канон cross-pack outline: ОТКАЗ от нового Blender-style outline, ВОЗВРАТ старого IRLEngine LocalLightOutline (Fresnel backlight-rim) во ВСЕ 6 паков (Photon первым 2026-06-28, остальные к 2026-06-29). В новых сессиях только улучшать старый, Blender-style не возвращать."
metadata:
  node_type: memory
  mod_scope: shader-inject
  type: project
  originSessionId: ba7b255e-5394-447f-be47-512086b5a68a
---

2026-06-28 РЕШЕНИЕ: в Photon-инжекте IRLite ОТКАЗАЛИСЬ от нового Blender-style outline (силуэт-Laplacian + crease + border, отдельный файл irlite_outline.glsl, параметры TARGET/WRAP/DEPTH_THRESHOLD/CREASE/CREASE_ANGLE) и ВЕРНУЛИ старый outline IRLEngine LocalLightOutline (Fresnel backlight-rim: 4-угловой depth-edge × Fresnel(1-|N·V|)^POWER × (1-NdotL) × attenuation × STRENGTH, albedo-tinted, per-light, tinted цветом света). В НОВЫХ СЕССИЯХ — ТОЛЬКО улучшать старый; Blender-style не возвращать.

КАНОН CROSS-PACK (расширено на все 6 паков к 2026-06-29):
- Распространено на ВСЕ 6 паков (BSL/Bliss/Complementary/IterationRP/Photon/Solas), in-game verified.
- Техника: OLD IRLEngine LocalLightOutline = Fresnel backlight-rim (4-tap edge depth × Fresnel(1-|N·V|)^POWER, per-light). Унифиц. параметры: PIXEL_SIZE 2 / STRENGTH 0.65 / FRESNEL_POWER 2.2, default OFF. Источник = архив IRLEngine/Patched/<pack>_IRLights.
- Per-pack дельты интеграции теперь живут в каждом *-pipeline.md (блок «Порт — выполнено»): Complementary=COMPOSITE1 gamma; BSL=linear+chocapic ld(); Solas=raw-linear+mirror-gate; Bliss=composite3 gate (TAA shimmer minor); IterationRP=PRE-albedo (AE-crater fix); Photon=inline d4 (см. ниже).

2026-06-29 РАСШИРЕНИЕ (front/back + glow; crease ОТКЛОНЁН): к старому outline добавлены ручки — BACK (слайдер, база заднего ободка), FRONT (bare-тоггл) + FRONT_STRENGTH (catch-light к источнику, ndl), GLOW (bare-тоггл) + GLOW_STRENGTH (мягкий Френель-halo внутрь силуэта, ловится bloom пака). Дефолты BACK 1.0 / FRONT on 0.3 / GLOW on 0.12; rim = BACK*(1-ndl) + FRONT_STRENGTH*ndl; contourFactor = silhouette + GLOW_STRENGTH*fres. Применено ко ВСЕМ 7 пакам Modification (6 канон + RethinkingVoxels — 7-й, не входил в исходный канон-6, но имеет тот же outline). Crease (детектор разрыва нормалей colortex4 -> внутр. контурные линии) был сделан в Complementary и ОТКЛОНЁН юзером — НЕ возвращать. Per-pack аккумулятор: Complementary/BSL/Bliss/RethinkingVoxels/Solas = irlite_outlineInk; Photon = outlineOut (имена irlFres/irlRim); IterationRP = litDiffuse pre-albedo (правки внутри #ifdef IRLITE_OUTLINE-блока). GUI: BACK/FRONT_STRENGTH/GLOW_STRENGTH в sliders=, FRONT/GLOW в outline-экране, lang en_* (ru_RU Bliss/Solas НЕ переведены -> англ. фолбэк). In-world verified: ПОДТВЕРЖДЕНО юзером (рантайм после crease-removal+порта, все паки). Все 7 injected-паков скопированы в Prism-инстанс (C:\prismlauncher\instances\BBS\minecraft\shaderpacks как <pack>_IRLights; плоские имена там = пристин, не трогать). Патчи: 5 шт (bsl/bliss/complementaryreimagined/rethinkingvoxels/solas) регенерированы генераторами, round-trip-валидированы (PatchHarness applier на Shadres/Original==Modification байт-в-байт, ex-штамп irlite_patched.txt) и ЗАКОММИЧЕНЫ — commit 27083f3 в bbs-irlights-addon master (вместе с фиксом sliders-tail ассертов их генераторов: FRESNEL_POWER->GLOW_STRENGTH). ЗАКРЫТО Photon (2026-06-29): (1) Photon-патч обновлён РУКАМИ в ОБОИХ (patches/photon.irlights + bbs-dof-addon/patches/photon-irl-dof.irlights, коуплинг), round-trip-валидирован (PatchHarness на чистом Original/Photon: photon.irlights == Modification байт-в-байт ex-штамп, 19 ops; DoF photon-irl-dof.irlights 36 ops — irlite_lights.glsl == Modification, IRL outline == Modification по word-diff, DoF-ops целы) и закоммичен (стиль 'IRLite patches: Photon outline front/back rim split + inner glow', trailer Co-Authored-By, НЕ пушено): bbs-irlights-addon master cef10f9, bbs-dof-addon master 51c8048. ЗАКРЫТО 2026-06-29 (история находки ниже): DoF-комбо в bbs-dof-addon есть у КАЖДОГО пака кроме RethinkingVoxels (RV-комбо закрыт 2026-07-01, теперь 7/7 -> [[rethinkingvoxels-pipeline]]) (= host-IRL-инжект +file irlite_lights.glsl + properties/lang ops, плюс DoF-слой), генератора НЕТ — правятся вручную. 5 комбо УСТАРЕЛИ (старый outline, BACK=0): bsl/bliss/complementaryreimagined/iterationrp/solas-irl-dof.irlights — при коммите 5 main-патчей (27083f3) DoF-сторона была пропущена, только photon-irl-dof обновлён. Обновлять host-IRL часть комбо по Modification/<pack> (источник) + main-патч как эталон дельты (27083f3) + photon-irl-dof как эталон правки комбо (51c8048); DoF-слой НЕ трогать; round-trip PatchHarness apply на Shadres/Original/<pack> (host-IRL==Modification, DoF-файлы целы); коммит в bbs-dof-addon master. ОТД. (ОБНОВЛЕНО 2026-07-09): IterationRP main iterationrp.irlights БОЛЬШЕ НЕ gitignore — автор Tahnass согласовал публичный патч, .gitignore-блок (был L133-135) убран, патч+генератор untracked и коммитабельны; патч актуализирован под новую версию пака (VolumetricFog +fogTransmittance) и round-trip diff-clean 12 ops (см. [[shader-iterationrp-pipeline]] + [[project-github-repos]]). Его комбо iterationrp-irl-dof в bbs-dof-addon отслеживается (не gitignore) — расхождение видимости снято. РЕШЕНО 2026-06-29: все 5 комбо синхронизированы (host-IRL: +file irlite_lights.glsl целиком заменён содержимым Modification/<pack> по readBody-правилу [тело = MOD.split('\n')]; outline screen-строка + sliders-tail + lang label/comment/+5 опций синканы из main-патчей по ключу; DoF-слой не тронут; ru_RU Bliss/Solas не трогали). Round-trip PatchHarness apply на чистом Shadres/Original/<pack>: apply ok, irlite_lights.glsl байт-в-байт == Modification, IRLITE_OUTLINE_* токены properties и lang-outline == Modification, DoF-файлы (irl_dof_*, focus-mask) целы (ед. ост. дельта properties = DoF-слайдер DOF_FOCUS_DISTANCE, ожидаемо). Закоммичено per-pack в bbs-dof-addon master (стиль 51c8048, НЕ пушено): BSL 73864a9, Bliss 9626585, ComplementaryReimagined b48aefd, Solas 85e6c95, IterationRP 88beb1f (юзер одобрил коммит itr-комбо; сам iterationrp.irlights тогда оставался gitignored — снято 2026-07-09, теперь коммитабелен). Гочи на будущее: (1) bbs-dof-addon БЕЗ .gitattributes — .irlights держатся LF через autocrlf clean-фильтр (git cat-file / git ls-files --eol = i/lf; git show <rev>:<path> ЛОЖНО показывает CRLF из-за smudge — не верить, проверять cat-file/ls-files); (2) comp-комбо нёс доп. не-outline дрейф (linearizeDepth -> view-space через gbufferProjectionInverse) — full-splice Modification его подтянул, per-hunk outline бы не хватило; (3) Modification/Photon = CRLF (file(1): "CRLF line terminators"), а патчер ВСЕГДА пишет тела +file в LF -> photon irlite_lights.glsl при round-trip отличается ТОЛЬКО EOL (контент идентичен, проверять diff --strip-trailing-cr; модифицируемые файлы properties/lang остаются CRLF, т.к. applier хранит dominant EOL пака -> совпадают строго). Касается ОБОИХ photon-патчей (main+combo), in-game безвредно. Прочие 6 паков = LF, glsl строго байт-в-байт. ФИНАЛ-АУДИТ 2026-06-29: все 7 главных IRL-патчей round-trip == Modification (GOLD --ignore-cr-at-eol; incl. iterationrp, ранее непроверенный) + все 6 combo host-IRL == Modification, DoF цел.

Состояние Shadres/Modification/Photon (= дев-пак run/shaderpacks/Photon, юзер перенёс из /1/):
- старый outline ВЖИВЛЁН inline в irlite_lights.glsl (функции irlite_outlineDepth + irlite_outlineFactor, БЕЗ отдельного файла; глубина через Photon combined_depth_tex + screen_to_view_space_depth; гард #ifdef IRLITE_OUTLINE && PROGRAM_DEFERRED4; outlineOut в irlite_lightSurface; в d4 добавляется albedo-tinted).
- параметры (старый набор): IRLITE_OUTLINE (toggle, default OFF), IRLITE_OUTLINE_PIXEL_SIZE 2, IRLITE_OUTLINE_STRENGTH 0.65, IRLITE_OUTLINE_FRESNEL_POWER 2.2; экран IRLights->Outline + слайдеры + lang.
- cookie/gobo сохранён (Modification до этого был УСТАРЕВШИМ без cookie — подтянут вперёд из патча/run перед правками).

ПАТЧЕРЫ ОБНОВЛЕНЫ (2026-06-28) — оба приведены к текущему Modification (старый outline + cookie, удалён +file irlite_outline.glsl):
- bbs-irlights-addon/patches/photon.irlights — GOLD-верифицирован: applier на ЧИСТЫЙ Shadres/Original/Photon == Shadres/Modification/Photon байт-в-байт (19 ops).
- bbs-dof-addon/patches/photon-irl-dof.irlights — КОМБО-патч «IRLite lights + DoF» (встраивает ТОТ ЖЕ IRL-инжект + DoF-операции на c2_dof). Обновлён тем же набором IRL-правок; apply OK (36 ops), DoF цел, IRL-часть (irlite_lights.glsl / d4 / properties / lang) == Modification.
КОУПЛИНГ: IRL-инжект Photon живёт в ДВУХ патчах — любые будущие правки инжекта вносить в ОБА (photon.irlights И bbs-dof-addon/.../photon-irl-dof.irlights), иначе DoF-патч дрейфует.
Верификация патча = javac pure-классов патчера из irl-core (IrlPatch/IrlPatchParser/IrlPatchApplier/PatchResult/PatchEngine — только java.*) + мини-harness Apply(patch, Original/Photon, out) -> diff --strip-trailing-cr out == Modification. Гоча: тело +file без trailing newline даёт косметический хвостовой diff — добавлять \n в конец тела.
Баг [[photon-bugfix]] B-OUTLINE-TRANSLUCENT — закрыт (новая фича удалена из Photon).

Источник старого outline (source of truth) = архив IRLEngine: C:\Users\Qualet\Media\Archive\AI_Slop_Projects\IRLEngine\Shaders\Patched\photon_IRLights (монолит shaders/include/lighting/ir_lights.glsl: LocalLightOutlineFactor ~L745, интеграция ~L1183-1191 и L1281-1356; settings.glsl LOCAL_LIGHT_OUTLINE_*). См. [[ref-irlengine-photon-patch]], [[shader-irlite-glsl]], [[photon-pipeline]].

Why: новый Blender-style outline не нравился и фактически не инкал BBS-модели (рендерятся через translucent-проход, в d4 их нет — баг B-OUTLINE-TRANSLUCENT); вернулись к проверенному IRLEngine-rim'у.
How: правки/улучшения старого outline -> Shadres/Modification/Photon/shaders/include/irlite/irlite_lights.glsl (+ d4/properties/lang), синк только Photon в run/shaderpacks/Photon, патчер актуализировать позже.
